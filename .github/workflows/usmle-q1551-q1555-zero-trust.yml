name: USMLE Q1551-Q1555 Zero Trust
# initial-q1551-q1555
on:
  push:
    paths:
      - '.github/workflows/usmle-q1551-q1555-zero-trust.yml'
permissions:
  contents: write
jobs:
  author-freeze-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      CAND_PATH: usmle/batch_specs_1501_1600/11_q1551_q1555_author_20260912.json
      DB_BLOB: b909be98a4f3861c443f3b188c2597a290b0b17a
      CANONICAL_COUNT: '1550'
      START_Q: '1551'
      END_Q: '1555'
      OUT_PATH: usmle/audit/Q1551_Q1555_ZERO_TRUST.json
      AUDIT_DATE: '20260912'
      FREEZE_DATE: '2026-09-12'
    steps:
      - uses: actions/checkout@v6
        with: {ref: main, fetch-depth: 0}
      - uses: actions/setup-python@v6
        with: {python-version: '3.12'}
      - name: Bind Q1550 production state
        run: |
          set -euo pipefail
          test "$(git hash-object usmle/data/usmle-step1.db)" = "$DB_BLOB"
          python - <<'PY'
          import json,os
          s=json.load(open('usmle/state/step2_final_q0001_q1550.json'))
          assert s['item_count']==s['step2_final_review_count']==1550
          assert s['contiguous_q0001_q1550'] is True and s['sqlite_integrity']=='ok'
          assert s['post_db_blob']==os.environ['DB_BLOB']
          PY
      - name: Author deterministic Q1551-Q1555
        run: |
          set -euo pipefail
          python -m py_compile usmle/author_q1551_q1555.py usmle/freeze_candidate_generic.py usmle/audit_candidate_zero_trust_current.py
          python usmle/author_q1551_q1555.py
          python - <<'PY'
          import json,os
          b=json.load(open(os.environ['CAND_PATH']))
          assert [x['num'] for x in b['items']]==list(range(1551,1556))
          assert b['canonical_count_before']==b['canonical_count_after']==1550
          assert b['canonical_db_blob']==os.environ['DB_BLOB'] and b['answer_key_sequence']=='ABCDE'
          assert len({x['drug'].casefold() for x in b['items']})==5
          assert 'ncjmm' not in open(os.environ['CAND_PATH']).read().casefold()
          assert all(x['sources'][0]['retrieved_at']=='2026-09-12' for x in b['items'])
          PY
      - name: Technical freeze from live declared sources
        run: |
          set -euo pipefail
          python usmle/freeze_candidate_generic.py
          echo "CAND_BLOB=$(git hash-object "$CAND_PATH")" >> "$GITHUB_ENV"
      - name: Zero-trust audit from zero against Q1550
        run: |
          set -uo pipefail
          set +e
          python usmle/audit_candidate_zero_trust_current.py
          RC=$?
          set -e
          echo "AUDIT_RC=$RC" >> "$GITHUB_ENV"
      - name: Persist blocked diagnostics
        if: env.AUDIT_RC != '0'
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add "$CAND_PATH" "$OUT_PATH"
          git commit -m 'usmle: persist blocked Q1551-Q1555 diagnostics' || true
          git pull --rebase origin main
          git push
      - name: Stop on blocked audit
        if: env.AUDIT_RC != '0'
        run: exit 1
      - name: Enforce 5 of 5 and build final manifest
        if: env.AUDIT_RC == '0'
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib,json,os,pathlib,subprocess
          a=pathlib.Path(os.environ['OUT_PATH']); r=json.load(open(a)); b=json.load(open(os.environ['CAND_PATH']))
          assert r['verdict']=='ZERO_TRUST_PASS' and r['failures']==[] and r['item_count']==5
          assert r['canonical_count']==r['canonical_review_count']==1550 and r['canonical_db_blob']==os.environ['DB_BLOB']
          assert all(x['status']=='PASS' and x['source_live_binding']=='PASS' and x['second_answer_attack']=='PASS' and x['blueprint']=='PASS' and x['evidence_derived_key']==x['intended_key'] and x['canonical_max_jaccard']<0.45 for x in r['item_reports'])
          keys=''.join(x['item']['intended_key'] for x in b['items']); assert keys=='ABCDE'; cand=os.environ['CAND_PATH']
          m={'audit_id':'Q1551-Q1555-FINAL-QA-PASS-20260912','status':'FINAL_QA_PASS','final_qa_verdict':'FINAL_QA_PASS_NO_MATERIAL_DEFECT','item_range':'Q1551-Q1555','item_count':5,'candidate_batch_blob':subprocess.check_output(['git','hash-object',cand],text=True).strip(),'candidate_batch_sha256':hashlib.sha256(pathlib.Path(cand).read_bytes()).hexdigest(),'authoritative_db_blob':os.environ['DB_BLOB'],'authoritative_db_final_count':1550,'authoritative_review_count':1550,'technical_freeze':'PASS_ALL_DECLARED_LOCATOR_COMPONENTS','zero_trust_audit_sha256':hashlib.sha256(a.read_bytes()).hexdigest(),'answer_key_sequence':keys,'all_items_final_10_10_pass':True,'unresolved_defects':[],'suggested_changes':[],'ncjmm':'NOT_APPLICABLE_USMLE','production_import_ready':True,'production_db_modified':False,'trusted_importer_complete':False}
          pathlib.Path('usmle/audit/Q1551_Q1555_FINAL_QA_PASS.json').write_text(json.dumps(m,indent=2,ensure_ascii=False)+'\n')
          PY
      - name: Re-read final gate and persist
        if: env.AUDIT_RC == '0'
        run: |
          set -euo pipefail
          test "$(git hash-object usmle/data/usmle-step1.db)" = "$DB_BLOB"
          python - <<'PY'
          import json
          m=json.load(open('usmle/audit/Q1551_Q1555_FINAL_QA_PASS.json'))
          assert m['status']=='FINAL_QA_PASS' and m['unresolved_defects']==[] and m['production_import_ready'] is True and m['production_db_modified'] is False
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add "$CAND_PATH" "$OUT_PATH" usmle/audit/Q1551_Q1555_FINAL_QA_PASS.json
          git commit -m 'usmle: finalize Q1551-Q1555 against Q1550 production'
          git pull --rebase origin main
          git push
