name: USMLE production pipeline (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. USMLE production is handled directly through ChatGPT + GitHub connector; no Copilot CLI credits are used."
